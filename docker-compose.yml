services:
  cn1:
    image: ghcr.io/gnosisguild/ciphernode:${VERSION:-latest}
    volumes:
      - ./configs/cn1.yaml:/home/ciphernode/.config/enclave/config.yaml:ro,Z
      - cn1-data:/home/ciphernode/.local/share/enclave
    extra_hosts:
      - "host.docker.internal:host-gateway"
    secrets:
      - secrets.json
    environment:
      RUST_LOG: "info"
      AGGREGATOR: "false"
    ports:
      - 9091:9091
    networks:
      - p2p_network

  cn2:
    image: ghcr.io/gnosisguild/ciphernode:${VERSION:-latest}
    volumes:
      - ./configs/cn2.yaml:/home/ciphernode/.config/enclave/config.yaml:ro,Z
      - cn2-data:/home/ciphernode/.local/share/enclave
    extra_hosts:
      - "host.docker.internal:host-gateway"
    secrets:
      - secrets.json
    depends_on:
      - cn1
    environment:
      RUST_LOG: "info"
      AGGREGATOR: "false"
    ports:
      - 9092:9092
    networks:
      - p2p_network

  cn3:
    image: ghcr.io/gnosisguild/ciphernode:${VERSION:-latest}
    volumes:
      - ./configs/cn3.yaml:/home/ciphernode/.config/enclave/config.yaml:ro,Z
      - cn3-data:/home/ciphernode/.local/share/enclave
    extra_hosts:
      - "host.docker.internal:host-gateway"
    secrets:
      - secrets.json
    depends_on:
      - cn1    
    environment:
      RUST_LOG: "info"
      AGGREGATOR: "false"
    ports:
      - 9093:9093
    networks:
      - p2p_network


  aggregator:
    image: ghcr.io/gnosisguild/ciphernode:${VERSION:-latest}
    volumes:
      - ./configs/agg.yaml:/home/ciphernode/.config/enclave/config.yaml:ro,Z
      - agg-data:/home/ciphernode/.local/share/enclave
    depends_on:
      - cn1
    extra_hosts:
      - "host.docker.internal:host-gateway"
    secrets:
      - secrets.json
    environment:
      RUST_LOG: "info"
      AGGREGATOR: "true"
    ports:
      - 9094:9094
    networks:
      - p2p_network


secrets:
  secrets.json:
    file: ./configs/secrets.json
    
volumes:
  cn1-data:
  cn2-data:
  cn3-data:
  agg-data:


networks:
  p2p_network:
    driver: bridge
