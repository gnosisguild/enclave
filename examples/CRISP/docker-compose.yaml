services:
  enclave-dev:
    build: .
    volumes:
      - .:/app
    entrypoint:
      - ""
    ports:
      - "8545:8545"
    # Keep container running in the background
    command: tail -f /dev/null
