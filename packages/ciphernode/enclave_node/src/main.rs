use eth::EtherClient;

fn main() {
	// boot up p2p network

	// boot up ether client

	// start main loop

	let ether = eth::EtherClient::new("test".to_string());
    println!("Hello, cipher world!");
}