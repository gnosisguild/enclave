[package]
name = "doclinker"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1.10"
walkdir = "2.4"
clap = { version = "4.4", features = ["derive"] }
globset = "0.4"

[dev-dependencies]
tempfile = "3.8"
